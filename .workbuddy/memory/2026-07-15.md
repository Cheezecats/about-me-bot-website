# 2026-07-15

## Final Ask James black-box revalidation (deployed site)

- Read-only acceptance test of deployed site https://cheezecats.github.io/about-me-bot-website/ via headless Chromium (Playwright, installed into managed node workspace `/Users/cheezecats/.workbuddy/binaries/node/workspace`, using cached chromium-1228 binary). No source/KB/config/env changed; only the report file created.
- Backend reachable via Cloudflare tunnel (`*.trycloudflare.com`) → Mac mini, HTTP 200. Chat works end-to-end.
- Ran 58 scripted UI interactions (S1–S7) + a fill pass for list answers / S1 UI / S3-1 re-test. Evidence: /tmp/chat_results.json, /tmp/chat_fill.json.
- Key findings:
  - Core Q&A excellent: S2 10/12, S3 15/15, S5 4/4 (context retained + topic-switch clean), S6 4/4, S7 8/8 (no privacy leak).
  - F2 (High): self-explanation/meta broken — 13/15 of S4 return topic-picker or profile-refusal (only "model" + "deployed" answer). Likely deployed Mac-mini backend lags the local updated build (meta intents worked in the v2 local test). No false claims (no fine-tune/web-search/permanent-history assertions).
  - F1 (High): landing page renders "404 Lost the frame" SPA route (Vite base path / GitHub Pages SPA fallback missing). Chat widget overlays and works.
  - F3/F4 (Medium): `james hobies` and `what did james write` refused (typo/phrasing gaps; essays exist).
  - F6/F7 (Low): "Recommend a game" blunt refusal; "what does james do for fun" → thin "cosplay for fun".
  - S3-1 transient "Failed to fetch" re-tested ×2 OK → tunnel blip, not a defect.
- Score 76/100. Report: WORKBUDDY_FINAL_CHAT_REVALIDATION_REPORT.md.

## Tooling note
- `agent-browser` skill is NOT installed in this env; used Playwright directly (project node_modules lacked it; installed `playwright-core@1.48.0` into the isolated managed workspace only).
