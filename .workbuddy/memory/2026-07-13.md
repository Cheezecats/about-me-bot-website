# 2026-07-13

## Adversarial boundary & robustness test of Ask James (read-only, HY3)
- 293 live POST /api/chat interactions across 12 categories (C1 informal 25, C2 typos 14, C3 follow-ups 70 over 7 topics, C4 compound 9, C5 comparison 11, C6 entity 14, C7 unsupported 14, C8 privacy 17, C9 injection 16, C10 language 11, C11 noisy 12, C12 determinism 8x10=80). Report: WORKBUDDY_ADVERSARIAL_CHATBOT_REPORT.md. Raw: /tmp/adversarial_results.json; analysis: /tmp/adversarial_analysis.json; harness: /tmp/adversarial_harness.py, /tmp/adversarial_analyze.py.
- VERDICT: P0=0, P1=0, P2=1, P3=many, P4=1. No privacy leak, no successful prompt injection, no crash, no hallucination. Determinism 8/8 groups 100% consistent. Latency median 3ms, p95 ~2.2s (Ollama gen tail), max 5.9s; no timeouts.
- P2: token "achievements" (and misspell "acheivements") fails to retrieve achievements chunks; "awards" works. So the earlier full-review "awards" P2 is RESOLVED — residual is narrower (achievements token). Fix: add achievements->award to QUERY_EXPANSIONS.
- P3 set: (a) slangy false refusals apex legends ank / songs he like / what songs do he like / musci james like (chunks exist); (b) Math IA & Physics IA first-turn questions misrouted to ambiguous_followup clarification picker (writing_math_ia_008, writing_ib_physics_ia_007 exist); (c) conversation degradation: follow-ups after 1st hop lose topic (What about it? -> topic picker; Which one? in sports session -> camera chunk); (d) wrong-topic substitution without ack: favorite photographer->camera gear, least favorite game->favorite games, favorite restaurant->favorite music (all grounded, not hallucinated); (e) C6 "What season did he reach it?" returned favorite season not Apex Season 22; (f) whole-compound "before 2020 and after 2020" refused; (g) Chinese-only queries refused (no CJK tokenization).
- P4: oversized/noisy payloads return HTTP 422 (request-too-large) = correct input validation, not crash.
- Positive: backend rate limiter active (MAX_REQUESTS_PER_MINUTE=60 per IP, sliding 60s) -> first harness burst got HTTP 429; re-ran paced (1.15s spacing + 62s cooldown) clean. response schema includes reason field (structured_fact/generated/model_refusal/privacy/no_retrieval/ambiguous_followup/rate_limited/compound).
- Did NOT modify any source/KB/config/env/tests; only created the report + /tmp scratch.

## Adversarial re-run v2 (updated setup: restart backend, verify query planner)
- Restarted backend to load dev's uncommitted fixes. /api/health now reports query_planner_enabled:true; server binds 127.0.0.1 (v1 P3 0.0.0.0 fixed). Chat response now exposes normalized_query + planner_used + planner_confidence (verified).
- 329 live interactions (249 base across 12 cats + 80 determinism). Report OVERWRITTEN: WORKBUDDY_ADVERSARIAL_CHATBOT_REPORT.md. Raw: /tmp/adversarial_results_v2.json; analysis: /tmp/adversarial_analysis_v2.json; harness: /tmp/adversarial_harness_v2.py, /tmp/adversarial_analyze_v2.py.
- VERDICT: P0=P1=P2=P3=P4 (classified failures) = 0. All v1 P2/P3 defects RESOLVED.
- Fixed-case groups ALL PASS: FxA Chinese 10/10 answered; FxB IA 5/5 specific (Math IA=Markov/Apex packet loss, Physics IA=FFT guitar tuner, EE=Uniswap V3); FxC second-hop camera/sports retain topic (games occasionally clarifies); FxD achievements 4/4 (acheivements now normalizes); FxE musci 4/4; FxF restaurant 2/2 clean refusal (no music substitution).
- planner_used=true on 247/249 (2 are oversized 422s rejected pre-planner). normalized_query present 247/249. Determinism 8/8 groups 100% consistent. Latency median 4ms, p95 1.89s, max 4.64s (planner avoids Ollama for most).
- P4 observations (non-blocking): (1) normalized_query for IA/essay Qs prepends topic hint e.g. "writing essays What is his Physics IA about" (answer correct); (2) "games" 2nd-hop sometimes clarifies via topic picker (acceptable). c8-09 returned public website URL, c8-17 returned public bio values — both safe, no leak.
- Did NOT modify any source/KB/config/env; only restarted backend (existing code) + created report + /tmp scratch.
