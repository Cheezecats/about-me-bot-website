# 2026-07-12

## QA test of "Ask James" chatbot (read-only, HY3 role)
- Ran full read-only test suite (62 requests) vs localhost stack (backend :8000, frontend :5173, Ollama qwen2.5:3b, reranker disabled/BM25).
- Report written: WORKBUDDY_CHATBOT_TEST_REPORT.md (in project root).
- Key findings: P1 = C8 hallucinated sports start ages (impossible, "19"); P2 cluster = games-topic failure ("What is James's favorite game?" refused 6/6 deterministically because BM25 ranks gaming_interest_in_game_development_007 above favorites_favorite_games_002 and clear-winner collapse sends only wrong chunk; short "favorite game" retrieved correct chunk but 3B model refused 5/6 => over-refusal). Other P2: B8 travelled/Traveled spelling, B9 instrument->guitar synonym, D6 games wrong topic, B7 name-chunk pollution ("Full Name project James Sui"), G4 unsupported "favorite language=Python", I2 flaky. P3: A3/A4 greetings refused, confidence always 0.0 (reranker-off side effect), frontend hardcoded localhost API URL + no vite proxy. Privacy (F1-F9) all safe incl. prompt injection.
- Did NOT modify any source/KB/config/env; only created the report. Raw JSON at /tmp/qa_results.json.
---

## Regression retest of Ask James chatbot (read-only, HY3, evening)
- Re-ran full read-only regression battery against latest code. Restarted backend (`.venv/bin/python main.py`, PID 40522) WITHOUT editing files to serve updated code. Reranker still disabled/BM25; 44 pytest pass; qwen2.5:3b present.
- Report: WORKBUDDY_CHATBOT_RETEST_REPORT.md (15 sections, ~370 lines). Raw JSON: /tmp/retest_results.json.
- VERDICT: all prior P1/P2 fixed. P1=0, P2=1, P3=3, P4=1.
- New P2: HALL-05 "What is James's favorite food and favorite season?" drops the food clause — compound splitter only splits when 2nd clause begins with a question word; "favorite season" after "and" has no question word so no split, only top-1 (season) answered. Fix: broaden split_compound_question.
- New P3: "Which sport did James start first?" now returns years list but not explicit "skiing" (structured Sports summary replaced LLM synthesis). Frontend still sends NO session_id (ChatBot.tsx posts {question} only). Confidence always 0.0/fallback always true (reranker off) persists. Prod API URL/no proxy persists.
- Fixed-vs-before: favorite game (6/6 refused -> structured lists, 10/10 repeat), travelled, instrument->guitar, games topic, projects "Full Name" pollution, favorite programming language now refused, small-talk hey there/how are you, P1 sports age hallucination gone, compound splitting + product-meta ("what model are you") are NEW working capabilities. Privacy 9/9 safe.
- Did NOT modify any source/KB/config/env; only created the report.
---

## Full engineering & product review of Ask James (read-only, HY3, per WORKBUDDY_FULL_REVIEW_PROMPT.md)
- 3rd review in series. Restarted backend WITHOUT editing (killed stale PID 45065 @20:31 predating answer.py edit @20:51; started PID 51923 serving current working-tree code). Health OK: reranker off, bm25 loaded. 47 pytest pass (up from 44). npm build OK (~1.46MB, chunk warning). qwen2.5:3b present.
- Report: WORKBUDDY_FULL_REVIEW_REPORT.md (15 sections). Raw data: /tmp/full_review_results.json (63 interactions), /tmp/consistency_results.json (10x5).
- VERDICT: P0=0, P1=0, P2=1, several P3, P4. ALL prior P1/P2 defects RESOLVED (dropped compound food clause HALL-05 fixed - S5-6 answers both; session_id NOW sent by ChatBot.tsx via crypto.randomUUID(); vite.config.ts NOW has /api->:8000 dev proxy). Determinism solved: 10/10 identical on all 5 repeat questions.
- ONLY new defect FN-01 (P2): "What awards has James won?" REFUSED though 5 achievements_* chunks exist (Physics Bowl Silver, CTB Top5%, Curieux, Lumiere, Qiu). Root cause = BM25 vocab mismatch: tokens 'awards'/'won'/'achievements' absent from chunks -> falls back to ubiquitous 'james' -> generic bio/contact/photo noise @~2.79. PROOF: query "Physics Bowl silver award" retrieves the chunk @29.76; bare "awards" & "achievements" both return 0 results. Fix: add awards->award, won->earned/award, achievements->award to QUERY_EXPANSIONS in tokenizer.py (+ optional Awards structured summary + regression test).
- P3: main.py binds 0.0.0.0 (should be 127.0.0.1); .env.local VITE_CHAT_API_URL=localhost baked into prod bundle (GH Pages deploy blocker); IA docs describe 3-stage DistilBERT reranker NOT shipped (models/reranker, data/eval_results.json, kb_private/ all MISSING); IA cites recall>=0.90 but shipped BM25 recall_at_10=0.858; test count doc says 15, actual 47. P4: generic "Part N" labels, README stale.
- Playwright E2E: no console errors, no failed requests, calls :8000, sends session_id. Safety S7 11/11 refused incl injection+inference; compound privacy isolation perfect (S5-3/S5-7). No hallucination (S8-5 Olympic medal correctly refused).
- Housekeeping note: reviewed behavior lives in UNCOMMITTED working tree (git status shows M across backend/frontend). Did NOT modify any source/KB/config/env; only created the report file.
